/* eslint-disable react/jsx-props-no-spreading */

import './Header.css';

import React, { FC } from 'react';

const Header: FC = () => {
  console.log('HEADER');

  return (
    <div>
      HEADER
    </div>
  );
};

export default Header;
